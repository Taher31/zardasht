---
import Layout from '../../layouts/Layout.astro';
import { getTranslation, resolveLang, withLanguagePrefix, languages } from '../../i18n';

export function getStaticPaths() {
  return languages.map((language) => ({ params: { lang: language.code } }));
}

const lang = resolveLang(Astro.params.lang);
const t = getTranslation(lang);
const categories = t.pages.home.categories.map((category) => ({
  ...category,
  href: withLanguagePrefix(lang, category.href),
}));
const industries = t.pages.home.industries;
const faq = t.pages.home.faq;
const trustBadges = t.pages.home.trustBadges;
---

<Layout title={t.pages.home.title} lang={lang} translation={t}>
  <section class="relative isolate overflow-hidden bg-gradient-to-br from-emerald-900 via-emerald-800 to-lime-700 text-white">
    <div class="absolute inset-y-0 right-0 -z-10 w-[26rem] translate-x-1/3 bg-white/10 blur-3xl sm:w-[32rem]"></div>
    <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6 sm:py-20 lg:py-24">
      <div class="grid grid-cols-1 items-center gap-12 lg:grid-cols-[1.1fr,0.9fr]">
        <div class="space-y-6 text-center lg:text-left">
          <p class="text-sm font-semibold uppercase tracking-[0.32em] text-emerald-100/80">{t.pages.home.eyebrow}</p>
          <h1 class="text-3xl font-semibold leading-tight sm:text-4xl lg:text-5xl">
            {t.pages.home.heroTitle}
          </h1>
          <p class="text-lg text-emerald-50/90 sm:text-xl">
            {t.pages.home.heroSubtitle}
          </p>
          <div class="flex flex-wrap items-center justify-center gap-3 lg:justify-start">
            <a class="pill-link bg-white text-emerald-900 shadow-lg shadow-emerald-900/30 transition hover:-translate-y-0.5 hover:shadow-xl" href={withLanguagePrefix(lang, '/contact')}>
              {t.pages.home.ctaPrimary}
            </a>
            <a class="pill-link border border-white/30 bg-white/10 text-white transition hover:bg-white/20" href={withLanguagePrefix(lang, '/products')}>
              {t.pages.home.ctaSecondary}
            </a>
          </div>

          <div class="flex flex-wrap items-start justify-center gap-3 lg:justify-start">
            {trustBadges.map((badge) => (
              <div class="flex items-start gap-2 rounded-full bg-white/10 px-4 py-3 text-left text-xs font-semibold uppercase tracking-[0.14em] ring-1 ring-white/20">
                <span class="mt-1 h-2 w-2 rounded-full bg-lime-300"></span>
                <div class="space-y-1 text-white">
                  <p>{badge.title}</p>
                  <p class="text-[11px] font-normal normal-case tracking-normal text-emerald-50/90">{badge.description}</p>
                </div>
              </div>
            ))}
          </div>

          <div class="grid grid-cols-1 gap-3 rounded-2xl bg-white/5 p-4 text-sm text-emerald-50/90 ring-1 ring-white/15 backdrop-blur sm:grid-cols-2">
            {t.pages.home.stats.map((stat) => (
              <div class="flex items-center gap-3 rounded-xl bg-white/5 p-3 ring-1 ring-white/10">
                <div class="flex h-10 w-10 items-center justify-center rounded-xl bg-white/10 text-base font-semibold">
                  {stat.value}
                </div>
                <div>
                  <p class="font-semibold">{stat.label}</p>
                </div>
              </div>
            ))}
          </div>
        </div>

        <div class="card relative overflow-hidden border border-white/10 bg-white/5 p-0 shadow-2xl shadow-emerald-900/30 ring-white/10 backdrop-blur">
          <div class="relative overflow-hidden">
            <img src="/hero-legumes.svg" alt={t.pages.home.heroImageAlt} class="h-64 w-full object-cover sm:h-72" loading="lazy" />
            <div class="absolute inset-0 bg-gradient-to-t from-emerald-900/70 via-transparent to-transparent"></div>
            <div class="absolute bottom-4 left-4 right-4 flex flex-wrap items-center justify-between gap-2 text-xs font-semibold uppercase tracking-[0.18em] text-white">
              <span class="inline-flex items-center gap-2 rounded-full bg-white/15 px-3 py-2 ring-1 ring-white/30">Quality First</span>
              <span class="inline-flex items-center gap-2 rounded-full bg-white/15 px-3 py-2 ring-1 ring-white/30">On-Time Shipment</span>
            </div>
          </div>
          <div class="relative space-y-4 p-6">
            <p class="text-sm font-semibold uppercase tracking-[0.26em] text-emerald-100/80">{t.pages.home.featuredTitle}</p>
            <ul class="space-y-3 text-sm text-emerald-50/90">
              {t.pages.home.featuredList.map((item) => (
                <li class="flex items-start gap-3">
                  <span class="mt-1 h-2.5 w-2.5 rounded-full bg-lime-300"></span>
                  {item}
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16">
    <div class="mb-10 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.28em] text-emerald-700/80">{t.pages.home.eyebrow}</p>
      <h2 class="section-title">{t.pages.home.categoriesTitle}</h2>
      <p class="mt-3 text-base text-slate-600 sm:text-lg">
        {t.pages.home.categoriesIntro}
      </p>
    </div>

    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4">
      {categories.map((category) => (
        <div class="card flex flex-col justify-between gap-5 p-6 transition hover:-translate-y-1 hover:shadow-2xl">
          <div class="space-y-4">
            <span class="inline-flex h-12 w-12 items-center justify-center rounded-full bg-emerald-50 text-sm font-semibold text-emerald-700">
              {category.badge}
            </span>
            <div class="space-y-2">
              <h3 class="text-xl font-semibold text-emerald-800">{category.name}</h3>
              <p class="text-slate-600">{category.description}</p>
            </div>
          </div>
          <a class="pill-link w-full border border-emerald-100 bg-emerald-50 text-emerald-700 hover:border-emerald-200 hover:bg-emerald-100" href={category.href}>
            {t.pages.home.ctaSecondary}
          </a>
        </div>
      ))}
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-4 pb-12 sm:px-6 sm:pb-16">
    <div class="mb-8 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.28em] text-emerald-700/80">{t.pages.home.eyebrow}</p>
      <h2 class="section-title">{t.pages.home.industriesTitle}</h2>
      <p class="mt-3 text-base text-slate-600 sm:text-lg">{t.pages.home.industriesIntro}</p>
    </div>
    <div class="grid grid-cols-1 gap-5 md:grid-cols-2">
      {industries.map((industry) => (
        <div class="card flex flex-col gap-3 p-5">
          <h3 class="text-lg font-semibold text-emerald-800">{industry.name}</h3>
          <p class="text-slate-600">{industry.description}</p>
        </div>
      ))}
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-4 pb-16 sm:px-6 sm:pb-20">
    <div class="mb-8 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.28em] text-emerald-700/80">{t.pages.home.eyebrow}</p>
      <h2 class="section-title">{t.pages.home.faqTitle}</h2>
      <p class="mt-3 text-base text-slate-600 sm:text-lg">{t.pages.home.faqIntro}</p>
    </div>
    <div class="space-y-4">
      {faq.map((item) => (
        <details class="card p-5">
          <summary class="flex cursor-pointer items-center justify-between gap-3 text-left text-base font-semibold text-emerald-800">
            <span>{item.question}</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-emerald-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.6" d="M12 4v16m8-8H4" />
            </svg>
          </summary>
          <p class="mt-3 text-sm text-slate-600">{item.answer}</p>
        </details>
      ))}
    </div>
    <div class="mt-8 text-center">
      <a class="pill-link border border-emerald-200 bg-emerald-600 text-white shadow-lg shadow-emerald-200 transition hover:-translate-y-0.5 hover:bg-emerald-700" href={withLanguagePrefix(lang, '/contact')}>
        {t.pages.home.ctaPrimary}
      </a>
    </div>
  </section>
</Layout>
