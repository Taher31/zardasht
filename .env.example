# Absolute public URL of the deployed site, e.g. https://www.example.com
# Required for @astrojs/sitemap to emit sitemap-index.xml, and used for the
# canonical / hreflang / Open Graph URLs in src/layouts/Layout.astro.
# Without it the build logs "Sitemap integration requires the `site` option".
SITE_URL=
