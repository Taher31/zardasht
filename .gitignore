# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
astro-dev.log
astro-dev-error.log


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Publish pipeline output (written by the admin panel, ~31MB per release).
# `current` is the symlink Nginx serves; both are build artefacts, not source.
releases/
current
